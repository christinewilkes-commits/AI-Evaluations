# ── Credentials — NEVER commit these ──────────────────────────────────────────
service-account.json
*.json.key
.env
*.env

# ── Node ──────────────────────────────────────────────────────────────────────
node_modules/
package-lock.json

# ── Generated report files (binary, not useful in git) ────────────────────────
*.docx

# ── Eval result CSVs — opt-in only ────────────────────────────────────────────
# To track a specific result CSV, use: git add -f results/my_run.csv
results/*.csv

# ── macOS ─────────────────────────────────────────────────────────────────────
.DS_Store
